//INPUT REG EX

pin = `[A-Z]{1}[0-9]{9,}[A-Z]{1}` // one letter 9 or more digits one letter
                                  // pattern = "[A-Z]{1}[0-9]{9,}[A-Z]{1}"

phone = `[0-9]{10,12}`     // digits only lenth btn 10 and 12 (inclusive)
                          // pattern = "[0-9]{10,12}"

staff = '[A-Za-z0-9]{5,}'; //alphanumeric 
                           // pattern = "[A-Za-z0-9]{5,}";

